const elements = ["Fire", "Air", "Water"];

const useJoin = elements.join("--");

console.log(useJoin);

//Split
const course = "Manipulación de Arrays";

const transformToArray = course.split(" ");

console.log(transformToArray);
